/** Unified presentation for the fallback glyph shown wherever a device/accessory/cable has no
 *  photo — a consistent 32×32 icon at 50% opacity everywhere (StaffChecklist, DeviceNode,
 *  Sidebar), instead of each component picking its own ad-hoc size/opacity. */
export const FALLBACK_ICON_CLASS = 'h-8 w-8 opacity-50';
